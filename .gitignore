.env
__pycache__/
*.pyc

# pipeline intermediates (regenerated each run; archived into outputs/ per run)
blocks.jsonl
classified.jsonl
extracted.json
review.txt
reconcile_report.txt

# generated reports
*.csv
output.txt

# pasted WhatsApp messages + the merged chat log (raw personal data, like inputs/)
raw/
